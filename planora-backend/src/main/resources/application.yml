spring:
  datasource:
    url: jdbc:mysql://localhost:3306/planora_db
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration